-- [[ dluu_07 HUB V1 - NEON EDITION ]] --

local Player = game.Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")

-- [ Function for Rounded Corners ] --
local function AddCorner(obj)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = obj
end

-- [ 1. NOTIFICATION ] --
local function ShowNotify()
    local NotifyGui = Instance.new("ScreenGui", CoreGui)
    local TextLabel = Instance.new("TextLabel", NotifyGui)
    TextLabel.Size = UDim2.new(1, 0, 0.2, 0)
    TextLabel.Position = UDim2.new(0, 0, 0.4, 0)
    TextLabel.BackgroundTransparency = 1
    TextLabel.TextColor3 = Color3.fromRGB(0, 255, 255) -- Cyan Color
    TextLabel.TextSize = 45
    TextLabel.Font = Enum.Font.GothamBold
    TextLabel.Text = ">> dluu_07 HUB ACTIVATED <<"
    
    -- Fade out effect
    task.delay(2, function()
        for i = 0, 1, 0.1 do
            TextLabel.TextTransparency = i
            task.wait(0.05)
        end
        NotifyGui:Destroy()
    end)
end
ShowNotify()

-- [ 2. GUI SETUP ] --
if CoreGui:FindFirstChild("dluu_07_HUB") then CoreGui.dluu_07_HUB:Destroy() end
local MainGui = Instance.new("ScreenGui", CoreGui)
MainGui.Name = "dluu_07_HUB"

local MainFrame = Instance.new("Frame", MainGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25) -- Deep Dark Blue/Black
MainFrame.Size = UDim2.new(0, 230, 0, 340)
MainFrame.Position = UDim2.new(0.35, 0, 0.3, 0)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.BorderSizePixel = 0
AddCorner(MainFrame)

-- Border Stroke Effect
local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = Color3.fromRGB(80, 0, 200) -- Purple Border
Stroke.Thickness = 2

local Title = Instance.new("TextLabel", MainFrame)
Title.Text = "dluu_07 HUB"
Title.Size = UDim2.new(1, 0, 0.12, 0)
Title.TextColor3 = Color3.fromRGB(0, 255, 255)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
Title.Font = Enum.Font.GothamBold
AddCorner(Title)

local TargetInput = Instance.new("TextBox", MainFrame)
TargetInput.PlaceholderText = "Enter Target Name..."
TargetInput.Size = UDim2.new(0.9, 0, 0.1, 0)
TargetInput.Position = UDim2.new(0.05, 0, 0.15, 0)
TargetInput.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
AddCorner(TargetInput)

local SpeedInput = Instance.new("TextBox", MainFrame)
SpeedInput.Text = "25"
SpeedInput.Size = UDim2.new(0.9, 0, 0.1, 0)
SpeedInput.Position = UDim2.new(0.05, 0, 0.27, 0)
SpeedInput.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
AddCorner(SpeedInput)

local function CreateBtn(text, pos, color)
    local btn = Instance.new("TextButton", MainFrame)
    btn.Text = text
    btn.Size = UDim2.new(0.9, 0, 0.12, 0)
    btn.Position = pos
    btn.BackgroundColor3 = color
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.BorderSizePixel = 0
    AddCorner(btn)
    return btn
end

-- Buttons with New Colors
local AntiBangBtn = CreateBtn("ANTI-BANG", UDim2.new(0.05, 0, 0.42, 0), Color3.fromRGB(50, 50, 80))
local FrontBtn = CreateBtn("MODE: FRONT", UDim2.new(0.05, 0, 0.56, 0), Color3.fromRGB(80, 0, 180))
local BackBtn = CreateBtn("MODE: BACK", UDim2.new(0.05, 0, 0.70, 0), Color3.fromRGB(100, 0, 220))
local StopBtn = CreateBtn("STOP EXECUTION", UDim2.new(0.05, 0, 0.85, 0), Color3.fromRGB(180, 0, 50))

-- Toggle Button (Floating Button)
local ToggleBtn = Instance.new("TextButton", MainGui)
ToggleBtn.Size = UDim2.new(0, 90, 0, 35)
ToggleBtn.Position = UDim2.new(0, 20, 0.45, 0)
ToggleBtn.Text = "dluu_07"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(80, 0, 200)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Draggable = true
AddCorner(ToggleBtn)

ToggleBtn.MouseButton1Click:Connect(function() 
    MainFrame.Visible = not MainFrame.Visible 
end)

-- [ CORE LOGIC ] --
local TargetPlayer = nil
local Mode = ""
local IsActive = false
local Angle = 0

local function GetPlayer(S)
    if S == "" then return nil end
    for _, v in pairs(game.Players:GetPlayers()) do
        if v.Name:lower():find(S:lower()) or v.DisplayName:lower():find(S:lower()) then return v end
    end
end

FrontBtn.MouseButton1Click:Connect(function()
    TargetPlayer = GetPlayer(TargetInput.Text)
    if TargetPlayer then Mode = "Front" IsActive = true Player.Character.Humanoid.Sit = true end
end)

BackBtn.MouseButton1Click:Connect(function()
    TargetPlayer = GetPlayer(TargetInput.Text)
    if TargetPlayer then Mode = "Back" IsActive = true Player.Character.Humanoid.Sit = true end
end)

StopBtn.MouseButton1Click:Connect(function() 
    IsActive = false 
    Mode = "" 
    if Player.Character:FindFirstChild("Humanoid") then Player.Character.Humanoid.Sit = false end
end)

AntiBangBtn.MouseButton1Click:Connect(function()
    local hrp = Player.Character.HumanoidRootPart
    local lastCF = hrp.CFrame
    hrp.CFrame = CFrame.new(0, -500, 0)
    task.wait(0.5)
    hrp.CFrame = lastCF
end)

RunService.RenderStepped:Connect(function()
    if IsActive and TargetPlayer and TargetPlayer.Character and Player.Character then
        local MyRoot = Player.Character:FindFirstChild("HumanoidRootPart")
        local TheirRoot = TargetPlayer.Character:FindFirstChild("HumanoidRootPart")
        
        if MyRoot and TheirRoot then
            local Speed = tonumber(SpeedInput.Text) or 25
            Angle = Angle + (Speed / 10)
            local MoveOffset = math.sin(Angle) * 1.5
            
            if Mode == "Front" then
                MyRoot.CFrame = TheirRoot.CFrame * CFrame.new(0, 0.9, -1.1) * CFrame.Angles(0, math.pi, 0) * CFrame.new(0, 0, MoveOffset)
            elseif Mode == "Back" then
                MyRoot.CFrame = TheirRoot.CFrame * CFrame.new(0, 0.9, 1.1) * CFrame.new(0, 0, MoveOffset)
            end
            MyRoot.Velocity = Vector3.new(0,0,0)
        end
    end
end)

