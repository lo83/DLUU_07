-- [[ dluu_07 HUB - 7yd7 EMOTE EDITION ]] --

local Player = game.Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")

-- [ 1. 7yd7 EMOTE FREE LOADSTRING ] --
-- ئەڤ پشکە دێ هەمی ئیمۆتان لۆد کەت و کەتە فری ب ڕێکا سورسێ 7yd7
task.spawn(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/7yd7/Hub/refs/heads/Branch/GUIS/Emotes.lua"))()
        print("7yd7 Emotes Loaded Successfully!")
    end)
end)

-- [ 2. GUI HELPER FUNCTIONS ] --
local function AddCorner(obj, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius or 8)
    corner.Parent = obj
end

local function AddStroke(obj, color, thickness)
    local stroke = Instance.new("UIStroke")
    stroke.Color = color
    stroke.Thickness = thickness
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Parent = obj
end

-- [ 3. NOTIFICATION ] --
local function ShowNotify()
    local NotifyGui = Instance.new("ScreenGui", CoreGui)
    local TextLabel = Instance.new("TextLabel", NotifyGui)
    TextLabel.Size = UDim2.new(1, 0, 0.2, 0)
    TextLabel.Position = UDim2.new(0, 0, 0.4, 0)
    TextLabel.BackgroundTransparency = 1
    TextLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
    TextLabel.TextSize = 40
    TextLabel.Font = Enum.Font.Code
    TextLabel.Text = ">> dluu_07 & 7yd7 EMOTES LOADED <<"
    task.delay(3, function() NotifyGui:Destroy() end)
end
ShowNotify()

-- [ 4. MAIN GUI SETUP ] --
if CoreGui:FindFirstChild("dluu_07_HUB") then CoreGui.dluu_07_HUB:Destroy() end
local MainGui = Instance.new("ScreenGui", CoreGui)
MainGui.Name = "dluu_07_HUB"

local MainFrame = Instance.new("Frame", MainGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
MainFrame.Size = UDim2.new(0, 240, 0, 360)
MainFrame.Position = UDim2.new(0.35, 0, 0.3, 0)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.BorderSizePixel = 0
AddCorner(MainFrame, 12)
AddStroke(MainFrame, Color3.fromRGB(0, 255, 150), 2) -- Neon Green for 7yd7 Style

local Title = Instance.new("TextLabel", MainFrame)
Title.Text = "dluu_07 x 7yd7"
Title.Size = UDim2.new(1, 0, 0.12, 0)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundColor3 = Color3.fromRGB(20, 30, 25)
Title.Font = Enum.Font.GothamBold
AddCorner(Title, 12)

local TargetInput = Instance.new("TextBox", MainFrame)
TargetInput.PlaceholderText = "Target Name..."
TargetInput.Size = UDim2.new(0.9, 0, 0.1, 0)
TargetInput.Position = UDim2.new(0.05, 0, 0.15, 0)
TargetInput.BackgroundColor3 = Color3.fromRGB(25, 35, 30)
TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
AddCorner(TargetInput, 6)

local SpeedInput = Instance.new("TextBox", MainFrame)
SpeedInput.Text = "25"
SpeedInput.Size = UDim2.new(0.9, 0, 0.1, 0)
SpeedInput.Position = UDim2.new(0.05, 0, 0.27, 0)
SpeedInput.BackgroundColor3 = Color3.fromRGB(25, 35, 30)
SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
AddCorner(SpeedInput, 6)

local function CreateBtn(text, pos, color, hoverColor)
    local btn = Instance.new("TextButton", MainFrame)
    btn.Text = text
    btn.Size = UDim2.new(0.9, 0, 0.11, 0)
    btn.Position = pos
    btn.BackgroundColor3 = color
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.BorderSizePixel = 0
    AddCorner(btn, 8)
    return btn
end

-- [ 5. BUTTONS ] --
local AntiBangBtn = CreateBtn("ANTI-BANG (FIXED)", UDim2.new(0.05, 0, 0.42, 0), Color3.fromRGB(40, 60, 50), Color3.fromRGB(60, 90, 70))
local FrontBtn = CreateBtn("MODE 1: FRONT", UDim2.new(0.05, 0, 0.55, 0), Color3.fromRGB(0, 150, 100), Color3.fromRGB(0, 200, 120))
local BackBtn = CreateBtn("MODE 2: BACK", UDim2.new(0.05, 0, 0.68, 0), Color3.fromRGB(0, 100, 150), Color3.fromRGB(0, 120, 200))
local StopBtn = CreateBtn("STOP ALL", UDim2.new(0.05, 0, 0.83, 0), Color3.fromRGB(150, 0, 0), Color3.fromRGB(200, 0, 0))

-- Toggle Button
local ToggleBtn = Instance.new("TextButton", MainGui)
ToggleBtn.Size = UDim2.new(0, 85, 0, 35)
ToggleBtn.Position = UDim2.new(0, 15, 0.5, 0)
ToggleBtn.Text = "7yd7 x dluu"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 120)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Draggable = true
AddCorner(ToggleBtn, 10)

ToggleBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

-- [ 6. LOGIC ] --
local TargetPlayer = nil
local Mode = ""
local IsActive = false
local Angle = 0

AntiBangBtn.MouseButton1Click:Connect(function()
    local hrp = Player.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        workspace.FallenPartsDestroyHeight = -1000
        local lastCFrame = hrp.CFrame
        hrp.CFrame = CFrame.new(Vector3.new(0, -500, 0))
        task.wait(0.7)
        hrp.CFrame = lastCFrame
        workspace.FallenPartsDestroyHeight = -500
    end
end)

local function GetPlayer(S)
    if S == "" then return nil end
    for _, v in pairs(game.Players:GetPlayers()) do
        if v.Name:lower():find(S:lower()) or v.DisplayName:lower():find(S:lower()) then return v end
    end
end

FrontBtn.MouseButton1Click:Connect(function()
    TargetPlayer = GetPlayer(TargetInput.Text)
    if TargetPlayer then Mode = "Front" IsActive = true Player.Character.Humanoid.Sit = true end
end)

BackBtn.MouseButton1Click:Connect(function()
    TargetPlayer = GetPlayer(TargetInput.Text)
    if TargetPlayer then Mode = "Back" IsActive = true Player.Character.Humanoid.Sit = true end
end)

StopBtn.MouseButton1Click:Connect(function() 
    IsActive = false 
    Mode = "" 
    if Player.Character:FindFirstChild("Humanoid") then Player.Character.Humanoid.Sit = false end
    if Player.Character:FindFirstChild("HumanoidRootPart") then Player.Character.HumanoidRootPart.Velocity = Vector3.new(0,0,0) end
end)

RunService.RenderStepped:Connect(function()
    if IsActive and TargetPlayer and TargetPlayer.Character and Player.Character then
        local MyRoot = Player.Character:FindFirstChild("HumanoidRootPart")
        local TheirRoot = TargetPlayer.Character:FindFirstChild("HumanoidRootPart")
        
        if MyRoot and TheirRoot then
            local Speed = tonumber(SpeedInput.Text) or 25
            Angle = Angle + (Speed / 10)
            local MoveOffset = math.sin(Angle) * 1.5
            
            if Mode == "Front" then
                MyRoot.CFrame = TheirRoot.CFrame * CFrame.new(0, 0.9, -1.1) * CFrame.Angles(0, math.pi, 0) * CFrame.new(0, 0, MoveOffset)
            elseif Mode == "Back" then
                MyRoot.CFrame = TheirRoot.CFrame * CFrame.new(0, 0.9, 1.1) * CFrame.new(0, 0, MoveOffset)
            end
            MyRoot.Velocity = Vector3.new(0,0,0)
        end
    end
end)
